import React from 'react';
import Header from '../components/Home/Header/Header';

const Home = () => {
    return (
        <main>
           <Header/> 
        </main>
    );
};

export default Home;